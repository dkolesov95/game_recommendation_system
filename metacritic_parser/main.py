

def main() -> None:
    """
    db = connect to db

    for year in range(2006, 2023): # years
        parser = class parser (year)
        pagination count pages = parser.get_pagination count pages()

        time.sleep()

        for padding_num in range(padding_count):
            data = parser.get_page_request(pagination count pages)
            db.send_data_to_db(data)

            time.sleep()
    :return:
    """
    pass


if __name__ == '__main__':
    main()
